import { OptionalSelectDirective } from './optional-select.directive';

describe('OptionalSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionalSelectDirective();
    expect(directive).toBeTruthy();
  });
});
