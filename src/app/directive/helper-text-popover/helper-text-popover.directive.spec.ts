import { HelperTextPopoverDirective } from './helper-text-popover.directive';

describe('HelperTextPopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new HelperTextPopoverDirective();
    expect(directive).toBeTruthy();
  });
});
