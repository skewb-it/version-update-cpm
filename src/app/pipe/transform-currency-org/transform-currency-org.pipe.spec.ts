import { TransformCurrencyOrgPipe } from './transform-currency-org.pipe';

describe('TransformCurrencyOrgPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformCurrencyOrgPipe();
    expect(pipe).toBeTruthy();
  });
});
